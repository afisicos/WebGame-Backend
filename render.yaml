services:
  - type: web
    name: game-backend
    runtime: node
    buildCommand: "npm install && npm run build"
    startCommand: "node dist/index.js"
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
